import { Typography } from "@mui/material";

const Analytics = () => {
    return (
        <div>
            <Typography variant="h4">Análises</Typography>
            <Typography variant="body1">
                Aqui estão os dados de análises do sistema.
            </Typography>
        </div>
    );
};

export default Analytics;
