import { Typography } from "@mui/material";

const Orders = () => {
    return (
        <div>
            <Typography variant="h4">Orders</Typography>
            <Typography variant="body1">
                Aqui estão as Orders
            </Typography>
        </div>
    );
};

export default Orders;
