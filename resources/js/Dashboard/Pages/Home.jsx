import { Typography } from "@mui/material";

const Home = () => {
    return (
        <div>
            <Typography variant="h4">Home</Typography>
            <Typography variant="body1">
                Home !
            </Typography>
        </div>
    );
};

export default Home;
